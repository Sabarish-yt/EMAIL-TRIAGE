[project]
name = "email-triage-openenv"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "gradio",
    "requests",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
